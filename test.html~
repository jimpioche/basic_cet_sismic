<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Calculateur Double</title>
</head>
<body>

  <label for="valeur">Entrez une valeur :</label>
  <input type="number" id="valeur" oninput="calculerDouble()">

  <p>Le double de la valeur est : <span id="resultat">0</span></p>

  <script>
    function calculerDouble() {
      // Récupérer la valeur de l'input
      var valeurInput = document.getElementById("valeur").value;

      // Calculer le double de la valeur
      var doubleValeur = valeurInput * 2;

      // Mettre à jour le contenu du span avec le résultat
      document.getElementById("resultat").innerText = doubleValeur;
    }
  </script>

</body>
</html>
